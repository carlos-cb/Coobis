{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>User creation</h1>
        <ol class="breadcrumb">
            <li><a href="{{ path('homepage') }}">Home</a></li>
            <li><a href="{{ path('user_index') }}">User list</a></li>
            <li class="active">User creation</li>
        </ol>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input value="Create" class="button button-pill" type="submit">
        <a href="{{ path('user_index') }}" class="button button-pill">Back to the list</a>
        {{ form_end(form) }}
    </div>
{% endblock %}
